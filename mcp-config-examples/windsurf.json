{
  "_comment": "Windsurf MCP config — add to ~/.codeium/windsurf/mcp_config.json or via Windsurf Settings → Cascade → MCP Servers",
  "_comment2": "Replace YOUR_API_KEY with the token returned by request_api_key(). Get one at https://qrveloz.com/settings",
  "mcpServers": {
    "qrveloz": {
      "serverUrl": "https://qrveloz.com/api/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
