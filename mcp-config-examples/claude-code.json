{
  "_comment": "Claude Code user-scope MCP config — add to ~/.claude/mcp.json",
  "_comment2": "Replace YOUR_API_KEY with the token returned by request_api_key(). Get one at https://qrveloz.com/settings",
  "mcpServers": {
    "qrveloz": {
      "type": "http",
      "url": "https://qrveloz.com/api/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
